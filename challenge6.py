# Conversión de Temperatura: Escribe un programa que convierta una temperatura dada en grados Celsius a grados Fahrenheit.

celsius = float(input("Grado Celsius: "))
convertir = 1.8 * celsius + 32
print(f'{convertir} °F')